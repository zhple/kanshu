# 仓库默认书 / 用户上传书

本目录的书籍会在 App 启动 / 检查更新 / 点「同步仓库书」时自动同步到用户书架。

## 用户从 App 上传

1. 在 GitHub 创建 Personal Access Token（需 `contents: write`）
2. App 顶栏设置图标里粘贴 Token
3. 对本地书打开菜单 → **上传到远程仓库**
4. 成功后会写入本目录文件，并更新 `catalog.json`

## 当前书目

见 `catalog.json`。
